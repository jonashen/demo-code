
- others.each_with_index do |other, index|
  - if index % 3 == 0
    .row
      %hr

  .col-xs-3.col-xs-offset-1.other
    .row
      = link_to (image_tag other.image, class: 'medium-book-image'), other_detail_path(other.id)
    .row
      = other.name